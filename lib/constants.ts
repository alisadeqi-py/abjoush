// Shared constants for horizontal scroll containers

/** Tailwind gap-4 = 1rem = 16px, used between cards in all carousels. */
export const CARD_GAP = 16;

/** Product card width: w-[210px] in Tailwind (ProductCard). */
export const PRODUCT_CARD_WIDTH = 210;

/** Testimonial card width: w-[290px] in Tailwind (Testimonials). */
export const TESTIMONIAL_CARD_WIDTH = 290;

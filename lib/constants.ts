// Shared constants for horizontal scroll containers

/** Tailwind gap-4 = 1rem = 16px, used between cards in all carousels. */
export const CARD_GAP = 16;

/** Product card dimensions: max-w-[220px] in Tailwind. */
export const PRODUCT_CARD_WIDTH = 220;

/** Testimonial card dimensions: max-w-[300px] in Tailwind. */
export const TESTIMONIAL_CARD_WIDTH = 300;
